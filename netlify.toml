[build]
  publish = "."

# Internal compliance docs live in the repo but must never be served.
# netlify.toml publishes the repo root, so without this rule
# /internal/ROADMAP.md would be publicly readable.
[[redirects]]
  from = "/internal/*"
  to = "/404.html"
  status = 404
  force = true
